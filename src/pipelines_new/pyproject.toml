[project]
name = "orders-by-customer-region"
version = "0.1.0"
requires-python = "~=3.12"
dependencies = ["bauplan~=0.1.16"]

[dependency-groups]
dev = [
    "ruff>=0.15",
    "ty>=0.0.48",
    "polars>=1",
]
