# Gold models are defined in src/pipelines/models.py (project root).
# Bauplan discovers models only at the project root level;
# the layer split is expressed through function order and comments in models.py.
